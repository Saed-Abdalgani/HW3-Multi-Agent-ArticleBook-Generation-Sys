# Markdown-first authoring

This chapter supports the book on **M5 Compile**. It follows the outline page budget and uses Markdown-first drafting so reviewers can comment before LaTeX conversion. Citations: [@markdownguide2024].

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

The pipeline separates knowledge in skills from execution in tools, keeping prompts auditable. Each stage persists artifacts under `content/` and `latex/` so reruns remain reproducible. Gatekeeper modules should own external calls with retries and rate limits drawn from configuration, not literals.

